//faça um algoritmo que calcule a área de um retangulo, solicitando ao usuário o valor da base e da altura. Para fins de cálculo, a fórmula da área de um retangulo é: a=(b*h)//

#include <stdio.h>
#include <stdlib.h>

int main(){
float a, b, h;

printf("Digite a base do retangulo: ");
	scanf("%f", &b);

printf("Digite a area do retangulo: ");
	scanf("%f", &h);
	
a = ("h * b");

printf("A area do retangulo é: %2.f\n", a);

}
